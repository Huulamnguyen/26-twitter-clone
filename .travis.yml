language: python

python:
  - 3.5
  - 3.6
  - 3.7
  - 3.9
before_install:
  - python --version
  - pip install -U pip
  - pip install -U unittest

install:
  - pip install ".[test]" . # install package + test dependencies

script: unittest # run tests